# Colab（A100）訓練環境精確 pin。
# 版本三元組已由 M2.1 spike 實測校正（2026-07 spike run，torch/trl/transformers/peft 皆確認可行）：
#   torch==2.11.0+cu128（Colab 內建 CUDA build，不覆蓋）| trl==1.8.0 |
#   transformers==5.12.1 | peft==0.19.1
trl==1.8.0
peft==0.19.1
transformers>=4.51
datasets>=3.0
accelerate>=1.0
# vllm：TRL 1.8.0 官方僅支援 0.16.0–0.23.0（spike 第一輪不 pin 時抓到最新 0.24.0，
# 直接炸 libcudart.so.13 找不到——0.24.0 要 CUDA 13 runtime，跟 Colab 內建 torch cu128 的
# CUDA 12.8 runtime 對不上）。pin 在支援區間內的最新版：
vllm==0.23.0
# torchao：Colab 預裝的 0.10.0 太舊，trl 1.8.0 的 vLLM 生成路徑要求 >0.16.0
# （spike 第二輪抓到：ImportError: Found an incompatible version of torchao）。
# 只給下界、不設上界——這是 trl 自己宣告的最低需求，讓 pip 挑最新相容版即可：
torchao>=0.16.0
# torch：用 Colab 內建 CUDA build，絕不 pin 覆蓋（專案 2 教訓）
