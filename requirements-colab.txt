# Colab（A100）訓練環境精確 pin。
# ⚠️ 版本三元組 (torch, vllm, trl) 以 M2.1 spike 實測為準——spike 通過後把可用組合固定在這裡。
# 初始候選（依 2026-07-10 查證：TRL v1.8.0 官方 openenv Wordle 範例即單 GPU colocate）：
trl==1.8.0
peft>=0.14
transformers>=4.51
datasets>=3.0
accelerate>=1.0
# vllm：以 TRL 1.8.0 setup.py 對應的 pin 為準（spike 時 pip install trl[vllm]==1.8.0 並記錄解出的版本）
# torch：用 Colab 內建 CUDA build，絕不 pin 覆蓋（專案 2 教訓）
