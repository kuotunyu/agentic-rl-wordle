# 本機 release/reviewer path（Windows/Linux，CPU）。
# constraints/dev.txt 精確鎖定 local/CI 工具；核心 runtime 仍為零依賴。
-c constraints/dev.txt
-e .[dev]

# 本機想跑 play.py / transformers 推理時再裝（可選）：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121   # 有 NVIDIA GPU
#   pip install -e ".[play]"
